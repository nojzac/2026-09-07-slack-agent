# Connecting Claude Code

- class: slack-agents
- chapter: 0 to 1
- title: Connecting Claude Code
- url: https://www.agenticcoding.school/v/w_JNpais
- durationSeconds: 510
- fetched: 2026-09-05
- transcriptChars: 7290

## Description (video page text, NOT transcript; may contain links)

**Links:**
- https://e2b.dev/
- The template: https://e2b.dev/docs/template/examples/claude-code

## Transcript

Okay. So in the last video we had our bot working whereby when we tag it, uh, then it will reply with a random number for us, kind of like this. And we basically want to now connect it up to Claude Code so it can actually start doing work and tasks for us. So essentially we need some kind of computer that runs on the cloud and can run for a long time, isolated to that particular task. And that is what a cloud sandbox is all about. Now basically everyone is kind of doing this in some way whereby they're using cloud sandboxes, either with a system they made themselves or they're using one of the many from providers kind of like e2b.dev, Daytona, Vercel Sandbox, or Cloudflare Sandboxes. Basically we're noticing in the market there are many more of these popping up, but I personally prefer e2b.dev for this kind of stuff because I find that network policy to be more customizable. Their agent also has a good time working with it as well. And you get a bunch of free credits to try out as well. So whichever provider you end up choosing, I'm going to be going through e2b in this video. Essentially they will have some kind of documentation that shows you how you can use your Claude Code agent inside of a sandbox. In the case of e2b, it looks kind of like this, and this will be linked down below. So you can see the way it works is we have a template which we define a node image. And then we install Claude Code, the latest version, onto it. We then customize how much CPU, virtual CPUs it has access to, memory, any kind of logging. And then we would create the sandbox with some environment variables defined. These environment variables would come from Vercel. And then we can run Claude Code in dangerously-skip-permissions with a command. And it can be fine with dangerously-skip-permissions in the sandbox because there is nothing sensitive there. And also when it comes to Anthropic API key over here, you can use an actual API key for API pricing, or you can use the Claude Code OAuth token, which we will be doing in this video, whereby you will be billed via your Claude Code subscription. Now, of course, they do have some terms of service regarding this being billed via your Claude Code subscription. So I'd recommend checking that out. And you can have Claude Code basically explain that to you. I think the general consensus is that it's fine to be billing via your Claude Code subscription provided you're the only person using it and it's not being shared amongst many other users. So anyways, what we can do is we can make an account on e2b.dev. So I will sign up over here and I'll go ahead and sign in with Google. And then after signing in, it should look kind of like this. So you can see how many sandboxes you're running right now and a bunch of other stats. And if you go over to billing, you will automatically have about $100 in credits for having a new account. So what we gotta do now is we gotta give this over to Claude Code and then basically tell it to set it up for us. So in a brand new chat inside of the same project for JoeStar, I'm gonna say, looking at the documentation here, can you basically change it so that when I tag JoeStar and give it some kind of message, then that is passed in as a prompt and then JoeStar will reply automatically. And this will be happening on an e2b container. And I will also tag the Claude Code Guide agent by doing claude-code-guide. This prompt will be down below as well. And then I will also say, can you make it so that it uses the OAuth token because it is private, private projects for me instead of the API key. So this gives it the basic functionality of we can give a prompt and then we have Claude Code running on the cloud and then it will reply to us as a message. And then we can start expanding on this even more whereby we can give it additional tooling such as the ability to send us attachments via Slack, which means that it could like create a HTML artifact and send it to us via Slack. We could give it stuff like being able to understand voice recordings by using some kind of like transcription service, ElevenLabs. And most importantly, we can give it access to GitHub as well. So it can start working on our projects, give it access to different MCP servers and tools. And this allows for all the powerful actions like I showed you before. And one nice thing that we can do with this template as well is we can add additional things such as Playwright. So Claude Code will be able to download and install Playwright and then start using a browser to kind of like test out features for us. We could also install Codex as well or make our own custom harness and have it deployed on Sandbox. Essentially, this is a really powerful setup that we can customize to our liking. In any way that we can dream of. If you wanted to, you could connect it to a database, have some kind of like permission control management system whereby certain channels can use certain MCP servers and other ones can't, or certain users have access to different tools and others don't. Essentially, when it comes to working with something like this, the sky's the limit. And because you own all the code, you own the infrastructure, you can just build out whatever feature you want. Now, what I personally do inside of my main version with Percy is that inside of percy-prs, I automatically get Percy to add new features to Percy and then deploy itself. So I basically said like, uh, since I have multiple Claude Code accounts and each thread is randomly assigned to a different account, so I can use multiple subscriptions. Can you make Percy resume switching to another OAuth token for Claude Code if it runs into an error? And that error being you hit your weekly limit. And then it basically explored the codebase and added that feature for me. Okay, it now seems that it added the ability whereby when we trigger the bot, then it will launch Claude Code with our message inside of a sandbox. So it says over here, boots the sandbox, runs Claude, passes the JSON output, and always kills the sandbox. But we got to do a couple of things so it's set up properly. So one of the things we have to do is set up a Claude token. So if we go to a brand new terminal session, and then run Claude setup-token. And now you want to keep this secret and not show to anyone else. And this token will be valid for one year. And I don't think there is an interface where you can like delete the token or like revoke it. So just make sure no one else gets their hands on your token. So we gotta go back to Vercel inside of our agent, go to environment variables, add environment variable, and the name should be claude-code-token.. And then we paste the value for the token inside of the value component. Keep it as sensitive and then press save. And now we got to set up the e2b API key. So let's go back to Vercel, add environment variable. The key should be e2b API key. Then going over to our dashboard, going to API keys, create API key. We can quickly make one. So I'm gonna call it like, uh, JoeStar. And I will copy this back into here. And then press save. So now we have all 4 environment variables set, the e2b one, Claude Code OAuth token, Slack bot token, and Slack bot signing secret. So you gotta get Claude to deploy our changes. And of course this code will be down below at the end of the video. So I'll just say commit, push, deploy, press enter. And it seems that it says that push-to-deploy is not wired up for this repo. So I will say wire up push-to-deploy and press enter as well. And now that it's deployed, we can finally go to Slack and tag JoeStar and say, who are you, question mark, press Enter. And now you can see it immediately says thinking... over here. And then it will reply with like, hey, I'm claude-code. But it seems we got an error. So let's quickly screenshot this and give it back to Claude. And of course, if you keep running into errors yourself, you may just want to download the final version of the code for the end of this video down below. Now in the next video, we will ideally want to be getting Claude to test JoeStar on our behalf instead. So I'll be going through that as well. Okay, so after a bit more back and forth, Claude found the issue, which was a stale template. And now if I go back to Slack, then it should ideally work. So if I then tag JoeStar and say, who are you? Then it will now do thinking over here, and then it should update the message with the reply from claude-code. So it should be like, I'm Claude made by Anthropic, kind of thing. And you can see it says that right over here. So now we have Claude running inside of a sandbox. So whenever we tag it with the prompt that we gave over here, so any message that we tag it with will be the prompt. Now in the next video, I will be showing you how you can make it so that Claude can automatically test your bot on your behalf. And this will really close a feedback loop for getting Claude to add brand new features.

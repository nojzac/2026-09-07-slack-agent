# Lesson 05 — connecting Claude Code

| File | What it is |
|---|---|
| `index.html` | **The SOP.** Why a sandbox, how the message flows now, and six steps — the first three are Noj's to do (E2B account, Claude token, 1Password). |

Transcript: `course/transcripts/05-0-to-1--connecting-claude-code.md`.
Code: `joestar-agent/api/_lib/claude.js`, `api/slack/events.js`, `e2b/template.mjs`.

Written before the lesson was finished, because steps 1–3 are account setup that
only Noj can do and the page is how those get explained. Steps 4–6 were written
from the code as built; tick them off against reality when they are run.
